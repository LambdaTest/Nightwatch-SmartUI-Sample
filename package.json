{
  "name": "nightwatch-js",
  "version": "0.0.1",
  "description": "NightWatch examples for LambdaTest",
  "main": "",
  "scripts": {
    "single": "cross-env CONFIG_FILE=single ./node_modules/.bin/nightwatch -e chrome tests",
    "parallel": "cross-env CONFIG_FILE=parallel ./node_modules/.bin/nightwatch -e chrome,edge,safari tests",
    "smartUI": "cross-env CONFIG_FILE=single ./node_modules/.bin/nightwatch tests/smartUI_test.js -e test1",
    "smartUI-ignoreDOM": "cross-env CONFIG_FILE=single ./node_modules/.bin/nightwatch tests/smartUI_ignoreDOM.js -e test1",
    "smartUI-fullPage": "cross-env CONFIG_FILE=single ./node_modules/.bin/nightwatch tests/smartUI_fullpageScreenshot.js -e test1"
  },
  "dependencies": {
    "@lambdatest/node-rest-client": "^1.0.5",
    "nightwatch": "^2.6.15"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
